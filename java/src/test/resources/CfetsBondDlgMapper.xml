<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.normal.trysth.mybatis.CfetsBondDlgMapper">


   <select id="selectAll" resultType="java.util.Map">
       select a.*
        from ttrd_cfets_trade_dlg a
        left join ttrd_cfets_trade_dlg_send s
        on a.quoteid = s.quoteid
        where a.clordid_client_id = '2233'
   </select>

</mapper>