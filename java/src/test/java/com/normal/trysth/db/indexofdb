1. explain：
    extra:
        use where: 根据条件在表中数据筛选
        use index: 直接返回数据，不需要回表
2. 优化器：基于执行成本，统计信息，选取最优的执行计划
3. 索引：索引由存储引擎实现，mysql 存在多个不同的执行引擎，因此索引的实现并没有统一的标准
        比如有些类型的索引只针对于特定的存储引擎

4. 读已提交&可重复读隔离级别下，如果没有索引的话，update 操作时，mysql会锁定可能涉及到的行，因为没有索引
不知道哪些行可能被改动嘛，干脆都锁起来.;